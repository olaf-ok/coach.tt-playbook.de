# i18n DE/EN/ES + Stroke-Type-Refactor — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** App spricht DE / EN / ES. Sprache wählbar in `/settings/language`, Default = Browser-Sprache mit Fallback DE. Schlagart-Tags (`AS`, `VH-TS`) werden zu sprachneutralen Codes (`SERVE`, `FH_TOPSPIN`) refactored und pro Sprache lokalisiert. Bestehende DB-Übungen werden migriert.

**Architecture:** Paraglide JS (compiler-based, tree-shakeable, typesafe) mit Strategien `['localStorage', 'preferredLanguage', 'baseLocale']`. Kein URL-Prefix. `setLocale()` reloadt — akzeptabel für Settings-Wechsel. Stroke-Type-Refactor: zwei Schichten (Codes intern, lokalisierte Labels via `m.*`). DB-Migration v2→v3 via Dexie-`upgrade`-Hook.

**Tech Stack:** SvelteKit 2 (Svelte 5 Runes, SPA-Mode `ssr=false`), `@inlang/paraglide-js` Vite-Plugin, Vitest (node-env, fake-indexeddb), Dexie 4.

**Spec:** `docs/superpowers/specs/2026-04-16-i18n.md`

---

## Task 1: Paraglide installieren + Projekt-Konfiguration

**Files:**
- Create: `project.inlang/settings.json`
- Create: `messages/de.json`
- Create: `messages/en.json`
- Create: `messages/es.json`
- Modify: `package.json` (devDependency `@inlang/paraglide-js`)
- Modify: `.gitignore` (Eintrag `src/lib/paraglide/`)

- [ ] **Step 1: Dependency installieren**

```bash
cd ~/Developer/tt-playbook-trainer
npm install --save-dev @inlang/paraglide-js
```

Expected: package.json hat neuen devDependency-Eintrag, `node_modules/@inlang/paraglide-js/` existiert.

- [ ] **Step 2: `project.inlang/settings.json` schreiben**

```json
{
  "$schema": "https://inlang.com/schema/project-settings",
  "baseLocale": "de",
  "locales": ["de", "en", "es"],
  "modules": [
    "https://cdn.jsdelivr.net/npm/@inlang/plugin-message-format@latest/dist/index.js"
  ],
  "plugin.inlang.messageFormat": {
    "pathPattern": "./messages/{locale}.json"
  }
}
```

- [ ] **Step 3: Initiale Message-Files anlegen mit Smoke-Message**

`messages/de.json`:
```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "smoke_test": "Hallo Welt"
}
```

`messages/en.json`:
```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "smoke_test": "Hello World"
}
```

`messages/es.json`:
```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "smoke_test": "Hola Mundo"
}
```

- [ ] **Step 4: `.gitignore` ergänzen**

Lies aktuelle `.gitignore`. Füge an passender Stelle (nach `node_modules`-Eintrag) folgende Zeile ein:

```
# Paraglide-Generierung
src/lib/paraglide/
```

- [ ] **Step 5: Commit**

```bash
git add package.json package-lock.json project.inlang/ messages/ .gitignore
git commit -m "chore(i18n): install paraglide-js, add de/en/es message files"
```

---

## Task 2: Vite-Plugin einbinden + app.html-lang-Placeholder

**Files:**
- Modify: `vite.config.ts`
- Modify: `src/app.html`

- [ ] **Step 1: Vite-Plugin importieren und konfigurieren**

Lies aktuelle `vite.config.ts`. Ersetze den kompletten Inhalt mit:

```typescript
import { defineConfig } from 'vitest/config';
import { sveltekit } from '@sveltejs/kit/vite';
import { SvelteKitPWA } from '@vite-pwa/sveltekit';
import { paraglideVitePlugin } from '@inlang/paraglide-js';

export default defineConfig({
	plugins: [
		paraglideVitePlugin({
			project: './project.inlang',
			outdir: './src/lib/paraglide',
			strategy: ['localStorage', 'preferredLanguage', 'baseLocale']
		}),
		sveltekit(),
		SvelteKitPWA({
			strategies: 'generateSW',
			registerType: 'autoUpdate',
			manifest: false,
			workbox: {
				globPatterns: ['client/**/*.{js,css,ico,png,svg,webmanifest}'],
				navigateFallback: '/'
			},
			devOptions: {
				enabled: false
			}
		})
	],
	test: {
		expect: { requireAssertions: true },
		environment: 'node',
		setupFiles: ['src/test-setup.ts'],
		include: ['src/**/*.{test,spec}.{js,ts}'],
		exclude: ['src/**/*.svelte.{test,spec}.{js,ts}']
	}
});
```

- [ ] **Step 2: `app.html` lang-Attribut auf Paraglide-Placeholder umstellen**

Lies aktuelle `src/app.html`. Ändere Zeile 2:

`<html lang="en">` → `<html lang="%paraglide.lang%" dir="%paraglide.dir%">`

- [ ] **Step 3: Build prüfen — Paraglide generiert `src/lib/paraglide/`**

```bash
cd ~/Developer/tt-playbook-trainer
npx vite build 2>&1 | tail -10
```

Expected: Build läuft durch, kein Paraglide-Fehler. `ls src/lib/paraglide/` zeigt mindestens `messages.js` und `runtime.js`.

- [ ] **Step 4: TypeScript-Check**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
```

Expected: 0 Errors / 0 Warnings.

- [ ] **Step 5: Commit**

```bash
git add vite.config.ts src/app.html
git commit -m "chore(i18n): add paraglide vite plugin, html lang placeholder"
```

---

## Task 3: Smoke-Test — eine bestehende Komponente nutzt `m.smoke_test`

**Files:**
- Modify: `src/routes/+page.svelte` (temporärer Test, wird in Task 4 wieder entfernt)

**Context:** Wir wollen verifizieren, dass `m.*`-Aufrufe im Browser funktionieren, bevor wir den Language-Store bauen. `/` redirected aktuell auf `/draw` — wir nutzen das Layout-`+layout.svelte` ist ungeeignet, weil zentral. Stattdessen ein temporärer Console-Log in der Root-Page.

- [ ] **Step 1: Console-Log mit Smoke-Message hinzufügen**

Lies aktuelle `src/routes/+page.svelte` (existiert vermutlich als Redirect). Falls sie ein `<script>` hat, füge dort `import { m } from '$lib/paraglide/messages'; console.log('i18n smoke:', m.smoke_test());` ein. Falls nur ein `goto`-Redirect drin steht, lass es so und packe den Smoke-Log nicht in eine Page, sondern füge ihn als temporären Effect in `src/routes/+layout.svelte` ein:

```svelte
// in <script> innerhalb onMount, ganz am Anfang:
import { m } from '$lib/paraglide/messages';
console.log('i18n smoke:', m.smoke_test());
```

- [ ] **Step 2: Dev-Server starten und prüfen**

```bash
cd ~/Developer/tt-playbook-trainer
npm run dev
```

Im Browser DevTools-Console öffnen. Erwartet: `i18n smoke: Hallo Welt` (DE-Browser) oder `Hello World` / `Hola Mundo` je nach `navigator.languages`.

- [ ] **Step 3: localStorage-Override testen**

In DevTools-Console:
```js
localStorage.setItem('PARAGLIDE_LOCALE', 'es');
location.reload();
```

Expected: Console zeigt `i18n smoke: Hola Mundo`. `<html lang="es">` im Inspector.

- [ ] **Step 4: Console-Log wieder entfernen**

Entferne den `console.log` und den `import { m }` aus der Datei wieder. Wir bauen den ordentlichen Wrapper in Task 4.

- [ ] **Step 5: Commit nicht nötig** (nur temporärer Test ohne bleibende Änderung)

---

## Task 4: Language-Store + Unit-Tests

**Files:**
- Create: `src/lib/i18n/language-store.svelte.ts`
- Create: `src/lib/i18n/language-store.test.ts`

- [ ] **Step 1: Test schreiben**

`src/lib/i18n/language-store.test.ts`:

```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';

const STORAGE_KEY = 'tt-language-mode';
const PARAGLIDE_KEY = 'PARAGLIDE_LOCALE';

vi.mock('$app/environment', () => ({ browser: true }));

const mockSetLocale = vi.fn();
const mockGetLocale = vi.fn(() => 'de');
vi.mock('$lib/paraglide/runtime', () => ({
  setLocale: (...args: unknown[]) => mockSetLocale(...args),
  getLocale: () => mockGetLocale()
}));

const mockReload = vi.fn();
beforeEach(() => {
  localStorage.clear();
  mockSetLocale.mockClear();
  mockReload.mockClear();
  Object.defineProperty(window, 'location', {
    configurable: true,
    value: { reload: mockReload }
  });
});

describe('language-store', () => {
  it('defaults to system mode when nothing stored', async () => {
    const { language } = await import('./language-store.svelte');
    expect(language.mode).toBe('system');
  });

  it('reads valid stored mode', async () => {
    localStorage.setItem(STORAGE_KEY, 'es');
    vi.resetModules();
    const { language } = await import('./language-store.svelte');
    expect(language.mode).toBe('es');
  });

  it('falls back to system on invalid stored value', async () => {
    localStorage.setItem(STORAGE_KEY, 'klingon');
    vi.resetModules();
    const { language } = await import('./language-store.svelte');
    expect(language.mode).toBe('system');
  });

  it('setLanguage("de") writes Paraglide localStorage and reloads', async () => {
    const { setLanguage } = await import('./language-store.svelte');
    setLanguage('de');
    expect(localStorage.getItem(STORAGE_KEY)).toBe('de');
    expect(mockSetLocale).toHaveBeenCalledWith('de');
  });

  it('setLanguage("system") removes Paraglide override and reloads', async () => {
    localStorage.setItem(PARAGLIDE_KEY, 'es');
    const { setLanguage } = await import('./language-store.svelte');
    setLanguage('system');
    expect(localStorage.getItem(PARAGLIDE_KEY)).toBeNull();
    expect(mockReload).toHaveBeenCalled();
  });
});
```

- [ ] **Step 2: Test laufen lassen — soll fehlschlagen**

```bash
npx vitest run src/lib/i18n/language-store.test.ts
```

Expected: Fail mit "Cannot find module './language-store.svelte'".

- [ ] **Step 3: Implementation**

`src/lib/i18n/language-store.svelte.ts`:

```typescript
import { browser } from '$app/environment';
import { setLocale, getLocale } from '$lib/paraglide/runtime';

export type LanguageMode = 'system' | 'de' | 'en' | 'es';

const STORAGE_KEY = 'tt-language-mode';
const PARAGLIDE_KEY = 'PARAGLIDE_LOCALE';

function readMode(): LanguageMode {
  if (!browser) return 'system';
  const v = localStorage.getItem(STORAGE_KEY);
  return v === 'de' || v === 'en' || v === 'es' || v === 'system' ? v : 'system';
}

export const language = $state({
  mode: readMode(),
  current: (browser ? getLocale() : 'de') as 'de' | 'en' | 'es'
});

export function setLanguage(mode: LanguageMode): void {
  language.mode = mode;
  localStorage.setItem(STORAGE_KEY, mode);

  if (mode === 'system') {
    localStorage.removeItem(PARAGLIDE_KEY);
    location.reload();
  } else {
    setLocale(mode);
  }
}
```

- [ ] **Step 4: Test laufen lassen — soll passen**

```bash
npx vitest run src/lib/i18n/language-store.test.ts
```

Expected: 5 tests passed.

- [ ] **Step 5: svelte-check**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
```

Expected: 0 Errors.

- [ ] **Step 6: Commit**

```bash
git add src/lib/i18n/
git commit -m "feat(i18n): add language store with mode persistence"
```

---

## Task 5: `/settings/language`-Seite + Sidebar-Eintrag aktivieren

**Files:**
- Create: `src/routes/settings/language/+page.svelte`
- Modify: `src/routes/settings/+layout.svelte` (Eintrag `available: false` → `true`)
- Modify: `messages/de.json`, `messages/en.json`, `messages/es.json` (neue Keys: `settings_language_*`, `lang_*`)

- [ ] **Step 1: Message-Keys ergänzen**

`messages/de.json` ergänzen (`smoke_test` darf weg):

```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "settings_title": "Einstellungen",
  "settings_language_title": "Sprache",
  "settings_language_hint": "Wähle die Sprache der App. „System" folgt der Browser-Sprache.",
  "settings_language_active": "Aktiv: {label}",
  "lang_option_system_label": "System",
  "lang_option_system_sub": "Folgt der Browser-Sprache",
  "lang_option_de_label": "Deutsch",
  "lang_option_en_label": "English",
  "lang_option_es_label": "Español"
}
```

`messages/en.json`:

```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "settings_title": "Settings",
  "settings_language_title": "Language",
  "settings_language_hint": "Choose the app language. \"System\" follows your browser language.",
  "settings_language_active": "Active: {label}",
  "lang_option_system_label": "System",
  "lang_option_system_sub": "Follows browser language",
  "lang_option_de_label": "Deutsch",
  "lang_option_en_label": "English",
  "lang_option_es_label": "Español"
}
```

`messages/es.json`:

```json
{
  "$schema": "https://inlang.com/schema/inlang-message-format",
  "settings_title": "Ajustes",
  "settings_language_title": "Idioma",
  "settings_language_hint": "Elige el idioma de la app. „Sistema" sigue el idioma del navegador.",
  "settings_language_active": "Activo: {label}",
  "lang_option_system_label": "Sistema",
  "lang_option_system_sub": "Sigue el idioma del navegador",
  "lang_option_de_label": "Deutsch",
  "lang_option_en_label": "English",
  "lang_option_es_label": "Español"
}
```

- [ ] **Step 2: `/settings/language/+page.svelte` schreiben**

Stilvorlage: `src/routes/settings/display/+page.svelte` (3-Optionen-Radio).

```svelte
<script lang="ts">
  import { m } from '$lib/paraglide/messages';
  import { language, setLanguage, type LanguageMode } from '$lib/i18n/language-store.svelte';

  interface Option {
    value: LanguageMode;
    label: string;
    hint: string;
  }

  const options: Option[] = [
    { value: 'system', label: m.lang_option_system_label(), hint: m.lang_option_system_sub() },
    { value: 'de', label: m.lang_option_de_label(), hint: 'DE' },
    { value: 'en', label: m.lang_option_en_label(), hint: 'EN' },
    { value: 'es', label: m.lang_option_es_label(), hint: 'ES' }
  ];

  const activeLabel = $derived(
    options.find((o) => o.value === language.mode)?.label ?? ''
  );
</script>

<section class="language">
  <h2>{m.settings_language_title()}</h2>
  <p class="hint">{m.settings_language_hint()}</p>

  <div class="options">
    {#each options as opt (opt.value)}
      <button
        type="button"
        class="option"
        class:active={language.mode === opt.value}
        onclick={() => setLanguage(opt.value)}
      >
        <span class="text">
          <span class="label">{opt.label}</span>
          <span class="hint">{opt.hint}</span>
        </span>
        <span class="radio" class:checked={language.mode === opt.value} aria-hidden="true"></span>
      </button>
    {/each}
  </div>

  <p class="note">{m.settings_language_active({ label: activeLabel })}</p>
</section>

<style>
  .language {
    padding: 40px;
    max-width: 640px;
    display: flex;
    flex-direction: column;
    gap: 20px;
  }
  h2 {
    font-size: 22px;
    font-weight: 600;
    color: var(--color-text-primary);
    margin: 0;
  }
  .hint {
    color: var(--color-text-secondary);
    font-size: 14px;
    margin: 0;
  }
  .options {
    background: var(--bg-surface);
    border: 1px solid var(--color-border);
    border-radius: var(--radius-panel);
    overflow: hidden;
  }
  .option {
    width: 100%;
    display: flex;
    align-items: center;
    gap: 14px;
    padding: 14px 16px;
    background: transparent;
    text-align: left;
    border-bottom: 1px solid var(--color-border);
  }
  .option:last-child { border-bottom: 0; }
  .option:hover { background: var(--color-chip-bg); }
  .option.active { background: var(--color-chip-bg); }
  .text { flex: 1; display: flex; flex-direction: column; gap: 2px; }
  .label { color: var(--color-text-primary); font-size: 15px; font-weight: 500; }
  .hint { color: var(--color-text-secondary); font-size: 12px; }
  .radio {
    width: 22px; height: 22px; border-radius: 50%;
    border: 2px solid var(--color-border); position: relative; flex-shrink: 0;
  }
  .radio.checked { border-color: var(--color-accent); }
  .radio.checked::after {
    content: ''; position: absolute; inset: 3px; border-radius: 50%;
    background: var(--color-accent);
  }
  .note { color: var(--color-text-secondary); font-size: 13px; margin: 0; }
</style>
```

- [ ] **Step 3: Sidebar-Eintrag aktivieren**

Lies `src/routes/settings/+layout.svelte`. Ändere Zeile 13:

`{ id: 'language', href: '/settings/language', label: 'Sprache', available: false },`
→
`{ id: 'language', href: '/settings/language', label: 'Sprache', available: true },`

- [ ] **Step 4: Build + Type-Check**

```bash
cd ~/Developer/tt-playbook-trainer
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
npx vite build 2>&1 | tail -5
```

Expected: 0 Errors, Build OK.

- [ ] **Step 5: Browser-Smoke-Test**

`npm run dev` → `/settings/language` öffnen. Vier Buttons sichtbar (System/DE/EN/ES). Klick auf „Español" → Reload, Texte spanisch, `<html lang="es">`.

- [ ] **Step 6: Commit**

```bash
git add messages/ src/routes/settings/language/ src/routes/settings/+layout.svelte
git commit -m "feat(i18n): add language settings page, activate sidebar entry"
```

---

## Task 6: Stroke-Type-Codes — neue Konstanten + Type

**Files:**
- Modify: `src/lib/constants/strokeTypes.ts` (komplett ersetzen)
- Modify: `src/lib/types/exercise.ts` (Stroke.strokeType-Type)
- Modify: `src/lib/constants/strokeTypes.test.ts` (Tests anpassen)

- [ ] **Step 1: Tests anpassen — fehlschlagen lassen**

`src/lib/constants/strokeTypes.test.ts` komplett ersetzen:

```typescript
import { describe, it, expect } from 'vitest';
import { DEFAULT_STROKE_TYPE_CODES, isStrokeTypeCode, type StrokeTypeCode } from './strokeTypes';

describe('DEFAULT_STROKE_TYPE_CODES', () => {
  it('enthält die 10 Default-Schlagarten als Codes', () => {
    const expected: StrokeTypeCode[] = [
      'SERVE', 'RECEIVE',
      'FH_TOPSPIN', 'BH_TOPSPIN',
      'FH_BLOCK', 'BH_BLOCK',
      'PUSH', 'FLIP',
      'FH_COUNTER', 'BH_COUNTER'
    ];
    expect(DEFAULT_STROKE_TYPE_CODES).toEqual(expected);
  });
});

describe('isStrokeTypeCode', () => {
  it('erkennt gültige Codes', () => {
    expect(isStrokeTypeCode('SERVE')).toBe(true);
    expect(isStrokeTypeCode('FH_TOPSPIN')).toBe(true);
    expect(isStrokeTypeCode('PUSH')).toBe(true);
  });

  it('lehnt ungültige Strings ab', () => {
    expect(isStrokeTypeCode('VH-TS')).toBe(false);
    expect(isStrokeTypeCode('AS')).toBe(false);
    expect(isStrokeTypeCode('xyz')).toBe(false);
    expect(isStrokeTypeCode('')).toBe(false);
  });
});
```

```bash
npx vitest run src/lib/constants/strokeTypes.test.ts
```

Expected: Fail (alte Exports `DEFAULT_STROKE_TYPES` / `findStrokeType` existieren noch, neue nicht).

- [ ] **Step 2: `strokeTypes.ts` komplett ersetzen**

```typescript
export type StrokeTypeCode =
  | 'SERVE'
  | 'RECEIVE'
  | 'FH_TOPSPIN'
  | 'BH_TOPSPIN'
  | 'FH_BLOCK'
  | 'BH_BLOCK'
  | 'PUSH'
  | 'FLIP'
  | 'FH_COUNTER'
  | 'BH_COUNTER';

export const DEFAULT_STROKE_TYPE_CODES: StrokeTypeCode[] = [
  'SERVE',
  'RECEIVE',
  'FH_TOPSPIN',
  'BH_TOPSPIN',
  'FH_BLOCK',
  'BH_BLOCK',
  'PUSH',
  'FLIP',
  'FH_COUNTER',
  'BH_COUNTER'
];

export function isStrokeTypeCode(v: unknown): v is StrokeTypeCode {
  return typeof v === 'string' && (DEFAULT_STROKE_TYPE_CODES as string[]).includes(v);
}
```

- [ ] **Step 3: `Stroke.strokeType`-Type schärfen**

Lies `src/lib/types/exercise.ts`. Ändere die Stroke-Interface so:

```typescript
import type { StrokeTypeCode } from '../constants/strokeTypes';

export interface Stroke {
  id: string;
  number: number;
  startPoint: Point;
  endPoint: Point;
  controlPoint: Point | null;
  strokeType: StrokeTypeCode | null;
  description: string | null;
}
```

(Den `StrokeType`-Interface aus diesem File entfernen — wird nicht mehr gebraucht. Falls dadurch andere Imports brechen, in Task 9 abgefangen.)

- [ ] **Step 4: Tests laufen lassen — sollen passen**

```bash
npx vitest run src/lib/constants/strokeTypes.test.ts
```

Expected: 2 tests passed.

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -10
```

Expected: viele neue TypeScript-Errors in Komponenten und seed.ts. Das ist erwartet — wir fangen sie in Tasks 7–9 ein.

- [ ] **Step 5: NICHT committen** — Tasks 6–9 gehören zusammen, sonst bleibt das Repo broken zwischendurch. Erster Commit am Ende von Task 9.

---

## Task 7: Stroke-Labels lokalisiert

**Files:**
- Create: `src/lib/i18n/stroke-type-labels.ts`
- Modify: `messages/de.json`, `messages/en.json`, `messages/es.json` (20 neue Keys: `stroke_<code>_short` + `stroke_<code>_full`)

- [ ] **Step 1: Message-Keys für alle 10 Stroke-Types ergänzen**

`messages/de.json` erweitern (zusätzlich zu den bestehenden Keys):

```json
"stroke_serve_short": "A",
"stroke_serve_full": "Aufschlag",
"stroke_receive_short": "RS",
"stroke_receive_full": "Rückschlag",
"stroke_fh_topspin_short": "VH-T",
"stroke_fh_topspin_full": "Vorhand-Topspin",
"stroke_bh_topspin_short": "RH-T",
"stroke_bh_topspin_full": "Rückhand-Topspin",
"stroke_fh_block_short": "VH-B",
"stroke_fh_block_full": "Vorhand-Block",
"stroke_bh_block_short": "RH-B",
"stroke_bh_block_full": "Rückhand-Block",
"stroke_push_short": "Schupf",
"stroke_push_full": "Schupf",
"stroke_flip_short": "Flip",
"stroke_flip_full": "Flip",
"stroke_fh_counter_short": "VH-K",
"stroke_fh_counter_full": "Vorhand-Konter",
"stroke_bh_counter_short": "RH-K",
"stroke_bh_counter_full": "Rückhand-Konter"
```

`messages/en.json` erweitern:

```json
"stroke_serve_short": "SV",
"stroke_serve_full": "Serve",
"stroke_receive_short": "REC",
"stroke_receive_full": "Receive",
"stroke_fh_topspin_short": "FH-T",
"stroke_fh_topspin_full": "Forehand Topspin",
"stroke_bh_topspin_short": "BH-T",
"stroke_bh_topspin_full": "Backhand Topspin",
"stroke_fh_block_short": "FH-BLK",
"stroke_fh_block_full": "Forehand Block",
"stroke_bh_block_short": "BH-BLK",
"stroke_bh_block_full": "Backhand Block",
"stroke_push_short": "Push",
"stroke_push_full": "Push",
"stroke_flip_short": "Flip",
"stroke_flip_full": "Flip",
"stroke_fh_counter_short": "FH-CTR",
"stroke_fh_counter_full": "Forehand Counter",
"stroke_bh_counter_short": "BH-CTR",
"stroke_bh_counter_full": "Backhand Counter"
```

`messages/es.json` erweitern:

```json
"stroke_serve_short": "SAQ",
"stroke_serve_full": "Saque",
"stroke_receive_short": "DEV",
"stroke_receive_full": "Devolución",
"stroke_fh_topspin_short": "D-T",
"stroke_fh_topspin_full": "Topspin de derecha",
"stroke_bh_topspin_short": "R-T",
"stroke_bh_topspin_full": "Topspin de revés",
"stroke_fh_block_short": "D-BLQ",
"stroke_fh_block_full": "Bloqueo de derecha",
"stroke_bh_block_short": "R-BLQ",
"stroke_bh_block_full": "Bloqueo de revés",
"stroke_push_short": "Push",
"stroke_push_full": "Push",
"stroke_flip_short": "Flip",
"stroke_flip_full": "Flip",
"stroke_fh_counter_short": "D-CONT",
"stroke_fh_counter_full": "Contraataque derecha",
"stroke_bh_counter_short": "R-CONT",
"stroke_bh_counter_full": "Contraataque revés"
```

- [ ] **Step 2: `stroke-type-labels.ts` schreiben**

```typescript
import { m } from '$lib/paraglide/messages';
import type { StrokeTypeCode } from '$lib/constants/strokeTypes';

export interface StrokeTypeLabel {
  code: StrokeTypeCode;
  short: string;
  full: string;
}

export function strokeTypeLabel(code: StrokeTypeCode): StrokeTypeLabel {
  switch (code) {
    case 'SERVE':       return { code, short: m.stroke_serve_short(),       full: m.stroke_serve_full() };
    case 'RECEIVE':     return { code, short: m.stroke_receive_short(),     full: m.stroke_receive_full() };
    case 'FH_TOPSPIN':  return { code, short: m.stroke_fh_topspin_short(),  full: m.stroke_fh_topspin_full() };
    case 'BH_TOPSPIN':  return { code, short: m.stroke_bh_topspin_short(),  full: m.stroke_bh_topspin_full() };
    case 'FH_BLOCK':    return { code, short: m.stroke_fh_block_short(),    full: m.stroke_fh_block_full() };
    case 'BH_BLOCK':    return { code, short: m.stroke_bh_block_short(),    full: m.stroke_bh_block_full() };
    case 'PUSH':        return { code, short: m.stroke_push_short(),        full: m.stroke_push_full() };
    case 'FLIP':        return { code, short: m.stroke_flip_short(),        full: m.stroke_flip_full() };
    case 'FH_COUNTER':  return { code, short: m.stroke_fh_counter_short(),  full: m.stroke_fh_counter_full() };
    case 'BH_COUNTER':  return { code, short: m.stroke_bh_counter_short(),  full: m.stroke_bh_counter_full() };
  }
}
```

- [ ] **Step 3: Build prüfen**

```bash
npx vite build 2>&1 | tail -5
```

Expected: Paraglide regeneriert `messages.js`, Build OK.

- [ ] **Step 4: NICHT committen** — gehört zu Tasks 6–9.

---

## Task 8: DB-Migration v2 → v3 + Seed auf Codes umstellen

**Files:**
- Create: `src/lib/db/migrations.ts`
- Create: `src/lib/db/migrations.test.ts`
- Modify: `src/lib/db/database.ts` (v3 mit upgrade-Hook)
- Modify: `src/lib/db/seed.ts` (`'VH-TS'` → `'FH_TOPSPIN'` etc.)

- [ ] **Step 1: Test für `migrateStrokeType` schreiben**

`src/lib/db/migrations.test.ts`:

```typescript
import { describe, it, expect } from 'vitest';
import { migrateStrokeType } from './migrations';

describe('migrateStrokeType', () => {
  it('mappt alte deutsche Strings auf Codes', () => {
    expect(migrateStrokeType('AS')).toBe('SERVE');
    expect(migrateStrokeType('RS')).toBe('RECEIVE');
    expect(migrateStrokeType('VH-TS')).toBe('FH_TOPSPIN');
    expect(migrateStrokeType('RH-TS')).toBe('BH_TOPSPIN');
    expect(migrateStrokeType('VH-Block')).toBe('FH_BLOCK');
    expect(migrateStrokeType('RH-Block')).toBe('BH_BLOCK');
    expect(migrateStrokeType('Schupf')).toBe('PUSH');
    expect(migrateStrokeType('Flip')).toBe('FLIP');
    expect(migrateStrokeType('VH-Konter')).toBe('FH_COUNTER');
    expect(migrateStrokeType('RH-Konter')).toBe('BH_COUNTER');
  });

  it('toleriert den Tippfehler VH-BL aus seed.ts', () => {
    expect(migrateStrokeType('VH-BL')).toBe('FH_BLOCK');
  });

  it('ist idempotent für bereits migrierte Codes', () => {
    expect(migrateStrokeType('FH_TOPSPIN')).toBe('FH_TOPSPIN');
    expect(migrateStrokeType('SERVE')).toBe('SERVE');
  });

  it('returns null für null oder unbekannte Werte', () => {
    expect(migrateStrokeType(null)).toBeNull();
    expect(migrateStrokeType('Unbekannt')).toBeNull();
    expect(migrateStrokeType('')).toBeNull();
  });
});
```

- [ ] **Step 2: Test laufen lassen — soll fehlschlagen**

```bash
npx vitest run src/lib/db/migrations.test.ts
```

Expected: Fail (Modul existiert nicht).

- [ ] **Step 3: `migrations.ts` schreiben**

```typescript
import { isStrokeTypeCode, type StrokeTypeCode } from '../constants/strokeTypes';

const LEGACY_DE_TO_CODE: Record<string, StrokeTypeCode> = {
  'AS': 'SERVE',
  'RS': 'RECEIVE',
  'VH-TS': 'FH_TOPSPIN',
  'RH-TS': 'BH_TOPSPIN',
  'VH-Block': 'FH_BLOCK',
  'RH-Block': 'BH_BLOCK',
  'VH-BL': 'FH_BLOCK',
  'Schupf': 'PUSH',
  'Flip': 'FLIP',
  'VH-Konter': 'FH_COUNTER',
  'RH-Konter': 'BH_COUNTER'
};

export function migrateStrokeType(legacy: string | null): StrokeTypeCode | null {
  if (!legacy) return null;
  if (isStrokeTypeCode(legacy)) return legacy;
  return LEGACY_DE_TO_CODE[legacy] ?? null;
}
```

- [ ] **Step 4: Tests laufen lassen — sollen passen**

```bash
npx vitest run src/lib/db/migrations.test.ts
```

Expected: 4 tests passed.

- [ ] **Step 5: Dexie-Schema v3 hinzufügen**

`src/lib/db/database.ts` ersetzen:

```typescript
import Dexie, { type Table } from 'dexie';
import type { Exercise } from '../types/exercise';
import type { Playlist } from '../types/playlist';
import { migrateStrokeType } from './migrations';

class TTPlaybookDB extends Dexie {
  exercises!: Table<Exercise, string>;
  playlists!: Table<Playlist, string>;

  constructor() {
    super('tt-playbook-trainer');
    this.version(1).stores({
      exercises: 'id, name, createdAt, updatedAt'
    });
    this.version(2).stores({
      exercises: 'id, name, createdAt, updatedAt',
      playlists: 'id, name, createdAt, updatedAt'
    });
    this.version(3)
      .stores({
        exercises: 'id, name, createdAt, updatedAt',
        playlists: 'id, name, createdAt, updatedAt'
      })
      .upgrade(async (tx) => {
        await tx
          .table('exercises')
          .toCollection()
          .modify((ex: Exercise) => {
            ex.strokes = ex.strokes.map((s) => ({
              ...s,
              strokeType: migrateStrokeType(s.strokeType as unknown as string | null)
            }));
          });
      });
  }
}

export const db = new TTPlaybookDB();
```

- [ ] **Step 6: `seed.ts` auf Codes umstellen**

In `src/lib/db/seed.ts` ersetzen:

- `strokeType: 'VH-TS'` → `strokeType: 'FH_TOPSPIN'` (3 Vorkommen)
- `strokeType: 'AS'` → `strokeType: 'SERVE'`
- `strokeType: 'RS'` → `strokeType: 'RECEIVE'`
- `strokeType: 'VH-BL'` → `strokeType: 'FH_BLOCK'`

(Das ist mit `Edit ... replace_all: true` pro Pattern machbar.)

- [ ] **Step 7: Tests + Build prüfen**

```bash
npx vitest run
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
```

Expected: alle Tests grün. svelte-check zeigt noch Errors in Komponenten (StrokeTypeButtons etc.) — wird in Task 9 gefixt.

- [ ] **Step 8: NICHT committen** — gehört zu Tasks 6–9.

---

## Task 9: Komponenten auf StrokeTypeCode umstellen

**Files:**
- Modify: `src/lib/components/StrokeTypeButtons.svelte`
- Modify: `src/lib/stores/currentExercise.svelte.ts` (Type-Signatur `assignStrokeType`)
- Modify: `src/lib/components/StepsPanel.svelte` (zeigt Tag-Label)
- Modify: `src/routes/archive/+page.svelte` (Filter-Chips)
- Modify ggf.: weitere Verwender — `grep -rn "shortLabel\|fullLabel\|DEFAULT_STROKE_TYPES\|findStrokeType"` nach diesem Refactor → 0 Treffer

- [ ] **Step 1: `StrokeTypeButtons.svelte` umbauen**

Aktuelle Komponente liest `DEFAULT_STROKE_TYPES` und gibt `shortLabel` als String an `onSelect` weiter. Komplett ersetzen durch:

```svelte
<script lang="ts">
  import { DEFAULT_STROKE_TYPE_CODES, type StrokeTypeCode } from '$lib/constants/strokeTypes';
  import { strokeTypeLabel } from '$lib/i18n/stroke-type-labels';

  interface Props {
    activeType: StrokeTypeCode | null;
    onSelect: (code: StrokeTypeCode) => void;
  }

  let { activeType, onSelect }: Props = $props();
</script>

<div class="buttons">
  {#each DEFAULT_STROKE_TYPE_CODES as code (code)}
    {@const label = strokeTypeLabel(code)}
    <button
      type="button"
      class="tag"
      class:active={activeType === code}
      title={label.full}
      onclick={() => onSelect(code)}
    >
      {label.short}
    </button>
  {/each}
</div>

<style>
  /* bestehende Styles aus dem Original übernehmen */
</style>
```

(Style-Block aus dem Originalfile beibehalten, nur Script + Markup ersetzen.)

- [ ] **Step 2: `currentExercise.svelte.ts` Signatur anpassen**

Lies `src/lib/stores/currentExercise.svelte.ts`. Suche `assignStrokeType`-Funktion und ändere:

```typescript
function assignStrokeType(id: string, code: StrokeTypeCode | null): void {
  updateStroke(id, { strokeType: code });
}
```

Plus Import oben:
```typescript
import type { StrokeTypeCode } from '$lib/constants/strokeTypes';
```

- [ ] **Step 3: `StepsPanel.svelte` Tag-Anzeige umbauen**

Suche im File die Stelle, die den aktuellen Tag eines Strokes anzeigt. Heute vermutlich direkt `stroke.strokeType` als String. Ersetze durch:

```svelte
<script lang="ts">
  import { isStrokeTypeCode } from '$lib/constants/strokeTypes';
  import { strokeTypeLabel } from '$lib/i18n/stroke-type-labels';
  // ...

  function tagLabel(strokeType: string | null): string {
    if (!strokeType || !isStrokeTypeCode(strokeType)) return '';
    return strokeTypeLabel(strokeType).short;
  }
</script>

<!-- statt: {stroke.strokeType} -->
{tagLabel(stroke.strokeType)}
```

(Genaue Stelle findet sich via `grep "strokeType" src/lib/components/StepsPanel.svelte`.)

- [ ] **Step 4: Archive-Filter-Chips umbauen**

Lies `src/routes/archive/+page.svelte`. Suche die Filter-Chip-Logik (vermutlich iteriert sie über `DEFAULT_STROKE_TYPES` oder hardcoded Strings). Ersetze durch:

```svelte
<script lang="ts">
  import { DEFAULT_STROKE_TYPE_CODES, type StrokeTypeCode } from '$lib/constants/strokeTypes';
  import { strokeTypeLabel } from '$lib/i18n/stroke-type-labels';
  // ...

  let activeFilters = $state<Set<StrokeTypeCode>>(new Set());
</script>

{#each DEFAULT_STROKE_TYPE_CODES as code (code)}
  {@const label = strokeTypeLabel(code)}
  <button
    type="button"
    class="chip"
    class:active={activeFilters.has(code)}
    onclick={() => toggleFilter(code)}
  >
    {label.short}
  </button>
{/each}
```

Filter-Vergleichslogik prüfen: `exercise.strokes.some(s => s.strokeType === filterCode)` muss sprachneutral funktionieren — durch das Code-Modell ist das automatisch der Fall.

- [ ] **Step 5: Restliche Verwender finden und fixen**

```bash
cd ~/Developer/tt-playbook-trainer
grep -rn "shortLabel\|fullLabel\|DEFAULT_STROKE_TYPES\|findStrokeType" src --include="*.svelte" --include="*.ts" | grep -v node_modules
```

Jeden Treffer einzeln prüfen und auf `StrokeTypeCode` / `strokeTypeLabel` umstellen.

- [ ] **Step 6: Tests + svelte-check + Build**

```bash
npx vitest run
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
npx vite build 2>&1 | tail -5
```

Expected: alle grün, 0 Errors, Build OK.

- [ ] **Step 7: Browser-Smoke-Test**

```bash
npm run dev
```

- `/draw`: Pfeil zeichnen → Tag-Buttons unten zeigen `A`, `RS`, `VH-T`, `RH-T`, …
- Bestehende Übung im `/archive` öffnen (gespeichert mit altem `'VH-TS'`-Tag) → Tag wird als `VH-T` angezeigt
- Archive-Filter „VH-T" findet die Übung

- [ ] **Step 8: Großer Commit für Tasks 6–9**

```bash
git add src/lib/constants/strokeTypes.ts src/lib/constants/strokeTypes.test.ts \
       src/lib/types/exercise.ts \
       src/lib/i18n/stroke-type-labels.ts \
       src/lib/db/migrations.ts src/lib/db/migrations.test.ts \
       src/lib/db/database.ts src/lib/db/seed.ts \
       src/lib/components/StrokeTypeButtons.svelte src/lib/components/StepsPanel.svelte \
       src/lib/stores/currentExercise.svelte.ts \
       src/routes/archive/+page.svelte \
       messages/de.json messages/en.json messages/es.json
git commit -m "refactor(strokes): introduce StrokeTypeCode + localized labels + db migration v3"
```

---

## Task 10: UI-Strings — Common, Sidebar, Settings-Shell

**Files:**
- Modify: `src/lib/components/Sidebar.svelte`
- Modify: `src/routes/settings/+layout.svelte`
- Modify: `messages/{de,en,es}.json` (neue Keys: `sidebar_*`, `settings_nav_*`, `common_*`)

**Vorgehensweise:** Pro File jeden hartkodierten deutschen String identifizieren, durch `m.<key>()` ersetzen, Key in alle drei Message-Files eintragen. Build nach jedem Commit prüfen.

- [ ] **Step 1: Keys in allen 3 Message-Files ergänzen**

`messages/de.json` zusätzlich:

```json
"common_brand_full": "TT Playbook Trainer",
"common_save": "Speichern",
"common_cancel": "Abbrechen",
"common_close": "Schließen",
"common_soon": "bald",
"common_pro_active": "Pro aktiv",
"common_free_badge": "Free",
"sidebar_brand_aria": "TT Playbook — Startseite",
"sidebar_tab_draw": "Zeichnen",
"sidebar_tab_archive": "Archiv",
"sidebar_tab_playlists": "Trainingslisten",
"sidebar_settings_aria": "Einstellungen",
"settings_nav_account": "Account",
"settings_nav_language": "Sprache",
"settings_nav_tv": "TV-Verbindung",
"settings_nav_display": "Anzeige",
"settings_nav_pro": "Pro-Abo",
"settings_nav_about": "Über"
```

`messages/en.json` zusätzlich:

```json
"common_brand_full": "TT Playbook Trainer",
"common_save": "Save",
"common_cancel": "Cancel",
"common_close": "Close",
"common_soon": "soon",
"common_pro_active": "Pro active",
"common_free_badge": "Free",
"sidebar_brand_aria": "TT Playbook — Home",
"sidebar_tab_draw": "Draw",
"sidebar_tab_archive": "Archive",
"sidebar_tab_playlists": "Training Lists",
"sidebar_settings_aria": "Settings",
"settings_nav_account": "Account",
"settings_nav_language": "Language",
"settings_nav_tv": "TV Connection",
"settings_nav_display": "Display",
"settings_nav_pro": "Pro Plan",
"settings_nav_about": "About"
```

`messages/es.json` zusätzlich:

```json
"common_brand_full": "TT Playbook Trainer",
"common_save": "Guardar",
"common_cancel": "Cancelar",
"common_close": "Cerrar",
"common_soon": "pronto",
"common_pro_active": "Pro activo",
"common_free_badge": "Free",
"sidebar_brand_aria": "TT Playbook — Inicio",
"sidebar_tab_draw": "Dibujar",
"sidebar_tab_archive": "Archivo",
"sidebar_tab_playlists": "Listas de entrenamiento",
"sidebar_settings_aria": "Ajustes",
"settings_nav_account": "Cuenta",
"settings_nav_language": "Idioma",
"settings_nav_tv": "Conexión TV",
"settings_nav_display": "Pantalla",
"settings_nav_pro": "Plan Pro",
"settings_nav_about": "Acerca de"
```

- [ ] **Step 2: `Sidebar.svelte` umstellen**

Im `<script>`-Block oben ergänzen: `import { m } from '$lib/paraglide/messages';`

Ersetzen:
- `label: 'Zeichnen'` → `label: m.sidebar_tab_draw()`
- `label: 'Archiv'` → `label: m.sidebar_tab_archive()`
- `label: 'Trainingslisten'` → `label: m.sidebar_tab_playlists()`
- `aria-label="TT Playbook — Startseite"` → `aria-label={m.sidebar_brand_aria()}`
- `aria-label="Einstellungen"` → `aria-label={m.sidebar_settings_aria()}`

- [ ] **Step 3: `settings/+layout.svelte` umstellen**

Import ergänzen. Ersetzen:
- `<h1>Einstellungen</h1>` → `<h1>{m.settings_title()}</h1>`
- `label: 'Account'` → `label: m.settings_nav_account()`
- `label: 'Sprache'` → `label: m.settings_nav_language()`
- `label: 'TV-Verbindung'` → `label: m.settings_nav_tv()`
- `label: 'Anzeige'` → `label: m.settings_nav_display()`
- `label: 'Pro-Abo'` → `label: m.settings_nav_pro()`
- `label: 'Über'` → `label: m.settings_nav_about()`
- `<span class="soon">bald</span>` → `<span class="soon">{m.common_soon()}</span>`

- [ ] **Step 4: Build + Type-Check**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
npx vite build 2>&1 | tail -5
```

Expected: 0 Errors.

- [ ] **Step 5: Commit**

```bash
git add messages/ src/lib/components/Sidebar.svelte src/routes/settings/+layout.svelte
git commit -m "i18n: extract common, sidebar, settings shell strings"
```

---

## Task 11: UI-Strings — Draw-Bereich (Toolbar, StepsPanel, Draw-Page)

**Files:**
- Modify: `src/lib/components/Toolbar.svelte`
- Modify: `src/lib/components/StepsPanel.svelte`
- Modify: `src/routes/draw/+page.svelte` (falls hartkodierte Strings)
- Modify: `src/lib/components/QrScanner.svelte` (falls Draw-relevant)
- Modify: `messages/{de,en,es}.json`

**Vorgehensweise wie Task 10.** Erst Keys ergänzen, dann pro File ersetzen.

- [ ] **Step 1: Strings in den Files erfassen**

```bash
cd ~/Developer/tt-playbook-trainer
grep -nE '>[A-ZÄÖÜa-zäöü][^<>{]{2,}<|placeholder=|aria-label=|title=' \
  src/lib/components/Toolbar.svelte \
  src/lib/components/StepsPanel.svelte \
  src/routes/draw/+page.svelte 2>/dev/null
```

Erwartete Strings (basierend auf Spec-Recherche):
- Toolbar: `+ Neu`, `Zurück`, `TV` (Status-Button)
- StepsPanel: `Wiederholungen`, `Dauer`, `Wählen` (placeholder), `Letzten Pfeil rückgängig` (title), `Zurück` (Undo-Label), `Keine Schläge eingezeichnet.`, Schlagart-Buttons (kommen über `strokeTypeLabel`)
- Draw-Page: `Tippe auf den Tisch oder ziehe einen Pfeil um zu starten.`

- [ ] **Step 2: Keys in allen 3 Message-Files**

`messages/de.json`:

```json
"toolbar_new_exercise": "+ Neu",
"toolbar_undo": "Zurück",
"toolbar_undo_aria": "Letzten Pfeil rückgängig",
"toolbar_tv_status_aria": "TV-Verbindung",
"steps_label_repeats": "Wiederholungen",
"steps_label_duration": "Dauer",
"steps_select_placeholder": "Wählen",
"steps_empty": "Keine Schläge eingezeichnet.",
"steps_freetext_label": "Notiz",
"steps_freetext_placeholder": "Freitext (optional)",
"draw_hint_empty": "Tippe auf den Tisch oder ziehe einen Pfeil um zu starten.",
"draw_save_success": "Gespeichert",
"draw_save_new": "Neue Übung",
"draw_save_error": "Fehler beim Speichern"
```

`messages/en.json`:

```json
"toolbar_new_exercise": "+ New",
"toolbar_undo": "Undo",
"toolbar_undo_aria": "Undo last arrow",
"toolbar_tv_status_aria": "TV connection",
"steps_label_repeats": "Repeats",
"steps_label_duration": "Duration",
"steps_select_placeholder": "Select",
"steps_empty": "No strokes drawn yet.",
"steps_freetext_label": "Note",
"steps_freetext_placeholder": "Free text (optional)",
"draw_hint_empty": "Tap the table or drag an arrow to start.",
"draw_save_success": "Saved",
"draw_save_new": "New exercise",
"draw_save_error": "Save failed"
```

`messages/es.json`:

```json
"toolbar_new_exercise": "+ Nuevo",
"toolbar_undo": "Deshacer",
"toolbar_undo_aria": "Deshacer última flecha",
"toolbar_tv_status_aria": "Conexión TV",
"steps_label_repeats": "Repeticiones",
"steps_label_duration": "Duración",
"steps_select_placeholder": "Elegir",
"steps_empty": "No hay golpes dibujados.",
"steps_freetext_label": "Nota",
"steps_freetext_placeholder": "Texto libre (opcional)",
"draw_hint_empty": "Toca la mesa o arrastra una flecha para empezar.",
"draw_save_success": "Guardado",
"draw_save_new": "Ejercicio nuevo",
"draw_save_error": "Error al guardar"
```

- [ ] **Step 3: Pro Datei alle hartkodierten Strings durch `m.*` ersetzen**

Vorgehen pro Datei:
1. `import { m } from '$lib/paraglide/messages';` oben im `<script>`.
2. Jeden gefundenen DE-String identisch zur Mapping-Tabelle oben ersetzen.
3. Falls ein String einen Wert interpoliert (z.B. Toast-Text mit Übungsname), Paraglide-Parameter-Syntax nutzen: `m.draw_save_success_named({ name: ex.name })` — falls solche Fälle auftauchen, Key entsprechend mit Parameter anlegen.

- [ ] **Step 4: Build + Browser-Test**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
npx vite build 2>&1 | tail -5
npm run dev
```

`/draw` aufrufen, alle Texte deutsch. Sprache wechseln auf EN → alle Draw-Texte englisch.

- [ ] **Step 5: Commit**

```bash
git add messages/ src/lib/components/Toolbar.svelte src/lib/components/StepsPanel.svelte src/routes/draw/
git commit -m "i18n: extract toolbar, steps-panel, draw-page strings"
```

---

## Task 12: UI-Strings — Archive

**Files:**
- Modify: `src/routes/archive/+page.svelte`
- Modify: `messages/{de,en,es}.json`

- [ ] **Step 1: Keys ergänzen**

`messages/de.json`:

```json
"archive_title": "Archiv",
"archive_search_placeholder": "Übungen suchen…",
"archive_empty_no_exercises": "Noch keine Übungen gespeichert.",
"archive_empty_no_match": "Keine Übung passt zu den aktuellen Filtern.",
"archive_action_rename": "Umbenennen",
"archive_action_duplicate": "Duplizieren",
"archive_action_delete": "Löschen",
"archive_filter_clear": "Filter zurücksetzen"
```

`messages/en.json`:

```json
"archive_title": "Archive",
"archive_search_placeholder": "Search exercises…",
"archive_empty_no_exercises": "No exercises saved yet.",
"archive_empty_no_match": "No exercise matches the current filters.",
"archive_action_rename": "Rename",
"archive_action_duplicate": "Duplicate",
"archive_action_delete": "Delete",
"archive_filter_clear": "Clear filters"
```

`messages/es.json`:

```json
"archive_title": "Archivo",
"archive_search_placeholder": "Buscar ejercicios…",
"archive_empty_no_exercises": "Aún no hay ejercicios guardados.",
"archive_empty_no_match": "Ningún ejercicio coincide con los filtros.",
"archive_action_rename": "Renombrar",
"archive_action_duplicate": "Duplicar",
"archive_action_delete": "Eliminar",
"archive_filter_clear": "Quitar filtros"
```

- [ ] **Step 2: `archive/+page.svelte` umstellen**

Import ergänzen, alle hartkodierten DE-Strings nach Tabelle ersetzen. Insbesondere:
- `<h1>Archiv</h1>` → `<h1>{m.archive_title()}</h1>`
- `placeholder="Übungen suchen…"` → `placeholder={m.archive_search_placeholder()}`
- `<p>Noch keine Übungen gespeichert.</p>` → `<p>{m.archive_empty_no_exercises()}</p>`
- `<p>Keine Übung passt zu den aktuellen Filtern.</p>` → `<p>{m.archive_empty_no_match()}</p>`
- `onclick={onRename}>Umbenennen` → `onclick={onRename}>{m.archive_action_rename()}`
- `onclick={onDuplicate}>Duplizieren` → `onclick={onDuplicate}>{m.archive_action_duplicate()}`
- `onclick={onDelete}>Löschen` → `onclick={onDelete}>{m.archive_action_delete()}`

(Genaue Stellen via `grep -n` in der Datei finden.)

- [ ] **Step 3: Build + svelte-check**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
npx vite build 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add messages/ src/routes/archive/
git commit -m "i18n: extract archive page strings"
```

---

## Task 13: UI-Strings — Trainingslisten

**Files:**
- Modify: `src/routes/playlists/+page.svelte`
- Modify: `src/lib/components/PlaylistDetail.svelte`
- Modify: `src/lib/components/PlaylistPlayer.svelte`
- Modify: `src/lib/components/PlaylistListItem.svelte`
- Modify: `src/lib/components/AddExerciseDialog.svelte`
- Modify: `messages/{de,en,es}.json`

- [ ] **Step 1: Keys ergänzen**

`messages/de.json`:

```json
"playlists_title": "Trainingslisten",
"playlists_empty_split": "Wähle oder erstelle eine Trainingsliste.",
"playlists_empty_list": "Noch keine Trainingsliste.",
"playlists_create": "+ Neue Trainingsliste",
"playlists_dialog_create_title": "Neue Trainingsliste",
"playlists_dialog_create_placeholder": "Name der Trainingsliste",
"playlists_action_rename": "Umbenennen",
"playlists_action_duplicate": "Duplizieren",
"playlists_action_delete": "Löschen",
"playlists_add_exercise": "+ Übung hinzufügen",
"playlists_add_dialog_title": "Übung hinzufügen",
"playlists_add_dialog_search": "Übungen suchen…",
"playlists_add_dialog_empty": "Keine verfügbaren Übungen.",
"playlists_remove_exercise": "Entfernen",
"playlists_play_on_tv": "Auf TV spielen",
"playlists_player_close": "Beenden",
"playlists_player_prev": "Vorherige",
"playlists_player_next": "Nächste",
"playlists_meta_count_one": "1 Übung",
"playlists_meta_count_other": "{count} Übungen"
```

`messages/en.json`:

```json
"playlists_title": "Training Lists",
"playlists_empty_split": "Pick or create a training list.",
"playlists_empty_list": "No training list yet.",
"playlists_create": "+ New training list",
"playlists_dialog_create_title": "New training list",
"playlists_dialog_create_placeholder": "Training list name",
"playlists_action_rename": "Rename",
"playlists_action_duplicate": "Duplicate",
"playlists_action_delete": "Delete",
"playlists_add_exercise": "+ Add exercise",
"playlists_add_dialog_title": "Add exercise",
"playlists_add_dialog_search": "Search exercises…",
"playlists_add_dialog_empty": "No exercises available.",
"playlists_remove_exercise": "Remove",
"playlists_play_on_tv": "Play on TV",
"playlists_player_close": "Stop",
"playlists_player_prev": "Previous",
"playlists_player_next": "Next",
"playlists_meta_count_one": "1 exercise",
"playlists_meta_count_other": "{count} exercises"
```

`messages/es.json`:

```json
"playlists_title": "Listas de entrenamiento",
"playlists_empty_split": "Elige o crea una lista de entrenamiento.",
"playlists_empty_list": "Aún no hay lista de entrenamiento.",
"playlists_create": "+ Nueva lista",
"playlists_dialog_create_title": "Nueva lista de entrenamiento",
"playlists_dialog_create_placeholder": "Nombre de la lista",
"playlists_action_rename": "Renombrar",
"playlists_action_duplicate": "Duplicar",
"playlists_action_delete": "Eliminar",
"playlists_add_exercise": "+ Añadir ejercicio",
"playlists_add_dialog_title": "Añadir ejercicio",
"playlists_add_dialog_search": "Buscar ejercicios…",
"playlists_add_dialog_empty": "No hay ejercicios disponibles.",
"playlists_remove_exercise": "Quitar",
"playlists_play_on_tv": "Reproducir en TV",
"playlists_player_close": "Detener",
"playlists_player_prev": "Anterior",
"playlists_player_next": "Siguiente",
"playlists_meta_count_one": "1 ejercicio",
"playlists_meta_count_other": "{count} ejercicios"
```

- [ ] **Step 2: Pro Datei `m.*`-Calls einsetzen**

Vorgehen wie in Tasks 10–12. Pro Datei `import { m }` ergänzen, dann jeden hartkodierten DE-String durch `m.<key>()` ersetzen.

- [ ] **Step 3: Build + svelte-check**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
npx vite build 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add messages/ src/routes/playlists/ src/lib/components/Playlist*.svelte src/lib/components/AddExerciseDialog.svelte
git commit -m "i18n: extract playlists strings"
```

---

## Task 14: UI-Strings — Settings-Subpages (about, display, pro, tv)

**Files:**
- Modify: `src/routes/settings/about/+page.svelte`
- Modify: `src/routes/settings/display/+page.svelte`
- Modify: `src/routes/settings/pro/+page.svelte`
- Modify: `src/routes/settings/tv/+page.svelte`
- Modify: `messages/{de,en,es}.json`

- [ ] **Step 1: Keys ergänzen**

`messages/de.json`:

```json
"settings_display_title": "Anzeige",
"settings_display_group": "Erscheinungsbild",
"settings_display_option_auto": "System",
"settings_display_option_auto_hint": "Folgt den Geräteeinstellungen.",
"settings_display_option_light": "Hell",
"settings_display_option_light_hint": "Hellgrauer Hintergrund, helle Karten.",
"settings_display_option_dark": "Dunkel",
"settings_display_option_dark_hint": "Dunkler Hintergrund, gut bei dunkler Halle.",
"settings_display_active": "Aktuell aktiv:",
"settings_display_active_light": "Hell",
"settings_display_active_dark": "Dunkel",
"settings_display_active_system_suffix": "(System)",
"settings_about_title": "Über",
"settings_about_version": "Version",
"settings_about_endpoint": "TV-Endpunkt",
"settings_about_stack": "Stack",
"settings_pro_title": "Pro-Abo",
"settings_pro_status_label": "Aktueller Plan",
"settings_pro_features_title": "Funktionen",
"settings_pro_dev_toggle_label": "Pro umschalten (Dev)",
"settings_tv_title": "TV-Verbindung",
"settings_tv_subtitle": "Tablet mit diesem Bildschirm verbinden",
"settings_tv_code_label": "Code",
"settings_tv_status_connecting": "Verbinde mit Server…",
"settings_tv_status_disconnected": "Verbindung geschlossen.",
"settings_tv_status_connecting_short": "Verbinde…",
"settings_tv_button_disconnect": "Trennen",
"settings_tv_button_connect": "Verbinden",
"settings_tv_input_placeholder": "Code eingeben"
```

`messages/en.json`:

```json
"settings_display_title": "Display",
"settings_display_group": "Appearance",
"settings_display_option_auto": "System",
"settings_display_option_auto_hint": "Follows device settings.",
"settings_display_option_light": "Light",
"settings_display_option_light_hint": "Light grey background, light cards.",
"settings_display_option_dark": "Dark",
"settings_display_option_dark_hint": "Dark background, ideal for dim halls.",
"settings_display_active": "Currently active:",
"settings_display_active_light": "Light",
"settings_display_active_dark": "Dark",
"settings_display_active_system_suffix": "(System)",
"settings_about_title": "About",
"settings_about_version": "Version",
"settings_about_endpoint": "TV endpoint",
"settings_about_stack": "Stack",
"settings_pro_title": "Pro plan",
"settings_pro_status_label": "Current plan",
"settings_pro_features_title": "Features",
"settings_pro_dev_toggle_label": "Toggle Pro (dev)",
"settings_tv_title": "TV connection",
"settings_tv_subtitle": "Connect tablet to this screen",
"settings_tv_code_label": "Code",
"settings_tv_status_connecting": "Connecting to server…",
"settings_tv_status_disconnected": "Connection closed.",
"settings_tv_status_connecting_short": "Connecting…",
"settings_tv_button_disconnect": "Disconnect",
"settings_tv_button_connect": "Connect",
"settings_tv_input_placeholder": "Enter code"
```

`messages/es.json`:

```json
"settings_display_title": "Pantalla",
"settings_display_group": "Apariencia",
"settings_display_option_auto": "Sistema",
"settings_display_option_auto_hint": "Sigue la configuración del dispositivo.",
"settings_display_option_light": "Claro",
"settings_display_option_light_hint": "Fondo gris claro, tarjetas claras.",
"settings_display_option_dark": "Oscuro",
"settings_display_option_dark_hint": "Fondo oscuro, ideal para salas con poca luz.",
"settings_display_active": "Activo:",
"settings_display_active_light": "Claro",
"settings_display_active_dark": "Oscuro",
"settings_display_active_system_suffix": "(Sistema)",
"settings_about_title": "Acerca de",
"settings_about_version": "Versión",
"settings_about_endpoint": "Punto TV",
"settings_about_stack": "Stack",
"settings_pro_title": "Plan Pro",
"settings_pro_status_label": "Plan actual",
"settings_pro_features_title": "Funciones",
"settings_pro_dev_toggle_label": "Cambiar Pro (dev)",
"settings_tv_title": "Conexión TV",
"settings_tv_subtitle": "Conecta la tablet con esta pantalla",
"settings_tv_code_label": "Código",
"settings_tv_status_connecting": "Conectando al servidor…",
"settings_tv_status_disconnected": "Conexión cerrada.",
"settings_tv_status_connecting_short": "Conectando…",
"settings_tv_button_disconnect": "Desconectar",
"settings_tv_button_connect": "Conectar",
"settings_tv_input_placeholder": "Introduce el código"
```

- [ ] **Step 2: Pro Datei umstellen**

Pro File `import { m }` ergänzen, hartkodierte DE-Strings ersetzen. Achtung: `display/+page.svelte` hat dynamische Werte (`theme.resolved === 'light' ? 'Hell' : 'Dunkel'`) — diese auf `m.settings_display_active_light()` / `m.settings_display_active_dark()` mappen.

- [ ] **Step 3: Build + svelte-check**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
npx vite build 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add messages/ src/routes/settings/{about,display,pro,tv}/
git commit -m "i18n: extract settings subpages strings"
```

---

## Task 15: UI-Strings — Account + Paywall

**Files:**
- Modify: `src/routes/settings/account/+page.svelte`
- Modify: `src/lib/components/PaywallDialog.svelte`
- Modify: `messages/{de,en,es}.json`

- [ ] **Step 1: Keys ergänzen**

`messages/de.json`:

```json
"account_title": "Account",
"account_tab_login": "Anmelden",
"account_tab_signup": "Registrieren",
"account_field_email": "E-Mail",
"account_field_password": "Passwort",
"account_submit_login": "Anmelden",
"account_submit_signup": "Registrieren",
"account_submit_loading": "Lädt…",
"account_logged_in_subscription_heading": "Abonnement",
"account_logged_in_manage_subscription": "Abo verwalten (bald)",
"account_logged_in_manage_subscription_hint": "Abo verwalten kommt mit der Stripe-Integration.",
"account_logged_in_logout": "Abmelden",
"account_mock_hint": "Noch kein Backend — Eingaben werden lokal gespeichert.",
"account_to_draw": "Neue Übung zeichnen",
"paywall_title": "Unbegrenzt Übungen mit TT Playbook Pro",
"paywall_feature_unlimited": "Unbegrenzte Übungen und Trainingslisten",
"paywall_feature_custom_tags": "Eigene Schlagart-Tags erstellen",
"paywall_feature_multidevice": "Mehrere Geräte synchronisieren",
"paywall_feature_community": "Frühe Features & Trainer-Community",
"paywall_plan_yearly": "Jährlich",
"paywall_plan_yearly_select": "Jährlich wählen",
"paywall_plan_monthly": "Monatlich",
"paywall_plan_monthly_select": "Monatlich wählen",
"paywall_plan_period_monthly": "pro Monat, jederzeit kündbar",
"paywall_popular_badge": "Beliebt",
"paywall_later": "Vielleicht später",
"paywall_existing_account": "Bereits ein Konto? Anmelden"
```

`messages/en.json`:

```json
"account_title": "Account",
"account_tab_login": "Sign in",
"account_tab_signup": "Sign up",
"account_field_email": "Email",
"account_field_password": "Password",
"account_submit_login": "Sign in",
"account_submit_signup": "Sign up",
"account_submit_loading": "Loading…",
"account_logged_in_subscription_heading": "Subscription",
"account_logged_in_manage_subscription": "Manage subscription (soon)",
"account_logged_in_manage_subscription_hint": "Subscription management arrives with Stripe.",
"account_logged_in_logout": "Sign out",
"account_mock_hint": "No backend yet — inputs are stored locally.",
"account_to_draw": "Draw new exercise",
"paywall_title": "Unlimited exercises with TT Playbook Pro",
"paywall_feature_unlimited": "Unlimited exercises and training lists",
"paywall_feature_custom_tags": "Create your own stroke tags",
"paywall_feature_multidevice": "Sync multiple devices",
"paywall_feature_community": "Early features & coach community",
"paywall_plan_yearly": "Yearly",
"paywall_plan_yearly_select": "Choose yearly",
"paywall_plan_monthly": "Monthly",
"paywall_plan_monthly_select": "Choose monthly",
"paywall_plan_period_monthly": "per month, cancel anytime",
"paywall_popular_badge": "Popular",
"paywall_later": "Maybe later",
"paywall_existing_account": "Already have an account? Sign in"
```

`messages/es.json`:

```json
"account_title": "Cuenta",
"account_tab_login": "Iniciar sesión",
"account_tab_signup": "Registrarse",
"account_field_email": "Correo",
"account_field_password": "Contraseña",
"account_submit_login": "Iniciar sesión",
"account_submit_signup": "Registrarse",
"account_submit_loading": "Cargando…",
"account_logged_in_subscription_heading": "Suscripción",
"account_logged_in_manage_subscription": "Gestionar suscripción (pronto)",
"account_logged_in_manage_subscription_hint": "La gestión de suscripción llega con Stripe.",
"account_logged_in_logout": "Cerrar sesión",
"account_mock_hint": "Aún no hay backend — las entradas se guardan localmente.",
"account_to_draw": "Dibujar ejercicio nuevo",
"paywall_title": "Ejercicios ilimitados con TT Playbook Pro",
"paywall_feature_unlimited": "Ejercicios y listas de entrenamiento ilimitados",
"paywall_feature_custom_tags": "Crea tus propios tags de golpes",
"paywall_feature_multidevice": "Sincroniza varios dispositivos",
"paywall_feature_community": "Funciones tempranas y comunidad de entrenadores",
"paywall_plan_yearly": "Anual",
"paywall_plan_yearly_select": "Elegir anual",
"paywall_plan_monthly": "Mensual",
"paywall_plan_monthly_select": "Elegir mensual",
"paywall_plan_period_monthly": "por mes, cancela cuando quieras",
"paywall_popular_badge": "Popular",
"paywall_later": "Quizá más tarde",
"paywall_existing_account": "¿Ya tienes una cuenta? Iniciar sesión"
```

- [ ] **Step 2: `account/+page.svelte` umstellen**

Pro Stelle `m.*` einsetzen. Insbesondere die Status-Badges (`Pro aktiv`, `Free`) nutzen die `common_*`-Keys aus Task 10.

- [ ] **Step 3: `PaywallDialog.svelte` umstellen**

Alle Stellen ersetzen. Achtung: Plan-Beschreibungen mit Preisangaben (z.B. `9,90 €/Monat`) bleiben hartkodierte Strings, weil Stripe-Preise später dynamisch kommen. Nur die Labels und Hinweise lokalisieren.

- [ ] **Step 4: Build + svelte-check**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
npx vite build 2>&1 | tail -5
```

- [ ] **Step 5: Commit**

```bash
git add messages/ src/routes/settings/account/ src/lib/components/PaywallDialog.svelte
git commit -m "i18n: extract account and paywall strings"
```

---

## Task 16: UI-Strings — TV + QR-Scanner

**Files:**
- Modify: `src/routes/tv/+page.svelte`
- Modify: `src/lib/components/TvDisplay.svelte`
- Modify: `src/lib/components/QrScanner.svelte`
- Modify: `messages/{de,en,es}.json`

- [ ] **Step 1: Keys ergänzen**

`messages/de.json`:

```json
"tv_pair_heading": "Gerät verbinden",
"tv_pair_qr_hint": "Scanne den QR-Code mit deinem Tablet oder gib den Code unten ein.",
"tv_pair_code_label": "Code",
"tv_pair_status_waiting": "Warte auf Tablet…",
"tv_pair_status_paired": "Verbunden",
"tv_display_no_exercise": "Keine Übung ausgewählt.",
"tv_meta_repeats": "Wiederholungen",
"tv_meta_duration": "Dauer",
"tv_meta_strokes_one": "1 Schlag",
"tv_meta_strokes_other": "{count} Schläge",
"qr_scanner_button": "QR scannen",
"qr_scanner_close": "Schließen",
"qr_scanner_error": "Kamera nicht verfügbar."
```

`messages/en.json`:

```json
"tv_pair_heading": "Connect device",
"tv_pair_qr_hint": "Scan the QR code with your tablet or enter the code below.",
"tv_pair_code_label": "Code",
"tv_pair_status_waiting": "Waiting for tablet…",
"tv_pair_status_paired": "Connected",
"tv_display_no_exercise": "No exercise selected.",
"tv_meta_repeats": "Repeats",
"tv_meta_duration": "Duration",
"tv_meta_strokes_one": "1 stroke",
"tv_meta_strokes_other": "{count} strokes",
"qr_scanner_button": "Scan QR",
"qr_scanner_close": "Close",
"qr_scanner_error": "Camera unavailable."
```

`messages/es.json`:

```json
"tv_pair_heading": "Conectar dispositivo",
"tv_pair_qr_hint": "Escanea el código QR con tu tablet o introduce el código abajo.",
"tv_pair_code_label": "Código",
"tv_pair_status_waiting": "Esperando tablet…",
"tv_pair_status_paired": "Conectado",
"tv_display_no_exercise": "Ningún ejercicio seleccionado.",
"tv_meta_repeats": "Repeticiones",
"tv_meta_duration": "Duración",
"tv_meta_strokes_one": "1 golpe",
"tv_meta_strokes_other": "{count} golpes",
"qr_scanner_button": "Escanear QR",
"qr_scanner_close": "Cerrar",
"qr_scanner_error": "Cámara no disponible."
```

- [ ] **Step 2: Files umstellen**

Pro Datei `import { m }`, jeden DE-String ersetzen.

- [ ] **Step 3: Build + svelte-check**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -5
npx vite build 2>&1 | tail -5
```

- [ ] **Step 4: Commit**

```bash
git add messages/ src/routes/tv/ src/lib/components/TvDisplay.svelte src/lib/components/QrScanner.svelte
git commit -m "i18n: extract tv display and qr scanner strings"
```

---

## Task 17: Final Verification + project-state.md

**Files:**
- Modify: `context/project-state.md` (im Workspace, nicht im Code-Repo)

- [ ] **Step 1: Verbleibende hartkodierte deutsche Strings finden**

```bash
cd ~/Developer/tt-playbook-trainer
grep -rnE '>[ZÄÖÜa-zäöü][^<>{]{2,}<' src --include="*.svelte" | \
  grep -vE 'TT Playbook|currentColor|aria-hidden|Co-Authored|test\.svelte' | head -30
```

Erwartet: 0 Treffer, oder nur erkennbare Brand-Strings / Code-Snippets in About-Page (Stack-Info kann einsprachig bleiben, ist technisch).

Falls noch Treffer: pro Treffer Key anlegen und durch `m.*` ersetzen, dann in alle drei Sprachen übersetzen, neuer Commit.

- [ ] **Step 2: Test-Suite komplett**

```bash
npm run test
```

Expected: alle Tests grün (mindestens 64 + neue: `+language-store` 5, `+migrations` 4, `+strokeTypes` 2 → ~75).

- [ ] **Step 3: svelte-check + Build**

```bash
npx svelte-check --tsconfig ./tsconfig.json 2>&1 | tail -10
NODE_OPTIONS='--max-old-space-size=1024' npx vite build 2>&1 | tail -10
```

Expected: 0 Errors / 0 Warnings, Build OK.

- [ ] **Step 4: Browser-E2E in 3 Sprachen**

```bash
npm run dev
```

Manuell durchklicken:

1. Browser-Sprache DE → App ist deutsch, Stroke-Tags `A`, `VH-T`, `Schupf`
2. `/settings/language` → Englisch wählen → Reload, alle Texte englisch, Tags `SV`, `FH-T`, `Push`
3. `/settings/language` → Spanisch wählen → Reload, Texte spanisch, Tags `SAQ`, `D-T`, `Push`
4. `/settings/language` → System wählen → Reload, zurück zur Browser-Sprache
5. Bestehende Übung mit altem `'VH-TS'`-Tag im Archive öffnen → Tag wird als `VH-T` (DE) / `FH-T` (EN) / `D-T` (ES) angezeigt
6. PaywallDialog auslösen (6. Übung als Free) → spanische Texte
7. Pairing /settings/tv → Code eingeben → Pair-Flow funktioniert in allen Sprachen

- [ ] **Step 5: project-state.md aktualisieren**

Datei: `/Users/olafkranz/Library/CloudStorage/GoogleDrive-info@olaf-kranz.de/Meine Ablage/ARBEIT/2_ TT-Playbook/AI - TT Playbook Trainer/context/project-state.md`

Aktualisieren:
- Datum oben auf `2026-04-16`
- Im Abschnitt **Phase-E-Teilabschluss** einen neuen Eintrag „**i18n DE/EN/ES + Stroke-Type-Refactor (2026-04-16):**" mit den Eckdaten:
  - Paraglide JS, Strategien `localStorage → preferredLanguage → baseLocale=de`
  - `/settings/language` (System/DE/EN/ES) funktional
  - Stroke-Tags refactored zu sprachneutralen Codes (`SERVE`, `FH_TOPSPIN`, …)
  - Lokalisierte Labels via `m.*`
  - DB-Migration v3 mappt alte DE-Strings auf Codes
  - 471 Übersetzungen in 3 Sprachen
  - Tests: ~75/75 grün, svelte-check 0/0/0
- Im Abschnitt **Nächste Schritte** „i18n-Grundgerüst (DE/EN/ES)" entfernen
- Im Abschnitt **Offene Punkte** ergänzen:
  - „Stroke-Notation Erweiterung: Banana/Strawberry Flick, Sidespin, Chop, Lob, Smash kommen bei Bedarf nach Trainer-Feedback"
  - „Custom Stroke-Types für Pro-User"
  - „Datums-/Zahlenformat mit `Intl.*` sobald Statistiken kommen"

- [ ] **Step 6: Commit (Code-Repo + Workspace)**

```bash
cd ~/Developer/tt-playbook-trainer
git add docs/superpowers/specs/2026-04-16-i18n.md docs/superpowers/plans/2026-04-16-i18n.md
git commit -m "docs(i18n): add spec and plan"

# Workspace ist nicht in Git, nur project-state.md updaten — keine Commit nötig.
```

- [ ] **Step 7: Live-Deploy**

```bash
cd ~/Developer/tt-playbook-trainer
git push mittwald main
```

Mittwald-Auto-Deploy zieht, baut, restartet. Nach ~60s https://trainer.tt-playbook.de prüfen — Sprach-Wechsel live testen (insbesondere mit echtem iPad in spanischer Locale).

---

## Self-Review Checkliste (vom Plan-Autor durchlaufen)

- ✅ Spec-Coverage: Paraglide-Setup (T1-3), Language-Store (T4), Sprach-UI (T5), Stroke-Refactor (T6-9), UI-String-Extraktion alle 22 Files (T10-16), Verification (T17)
- ✅ Keine Placeholder „TODO" / „später" / „ähnlich" — jeder Task enthält Code oder konkrete Edit-Anweisungen mit DE→Key-Mapping
- ✅ Type-Konsistenz: `StrokeTypeCode` durchgängig in T6-9, `LanguageMode` in T4-5
- ✅ Storage-Migration via Dexie-`upgrade` deckt Edge-Cases (alter String, neuer Code, null, Tippfehler `VH-BL`)
- ✅ Tests vor Implementation in TDD-Tasks (T4, T6, T8)
